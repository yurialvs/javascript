// for(variavel; condicional; incremento) {}

for (let n = 1; n <= 3; n++) {
    console.log("Frase teste " + n);
}

// Loop for em arrays

let cores = ["preto", "branco", "azul", "vermelho"];

for (let c = 0; c < cores.length; c++) {
    console.log(cores[c]);
}

// Outro exemplo:

let bolos = ["chocolate", "coco", "milho"];

for (let b in bolos) {
    console.log(bolos[b]);
}

